<UserControl x:Class="Tauron.Application.CelloManager.UI.Views.MainWindow.SpoolView.SpoolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:hlp="http://schemas.Tauron-Hobby-Studio.org/xaml/presentation"
             xmlns:contracts="clr-namespace:Tauron.Application.CelloManager;assembly=Tauron.Application.CelloManager.Contracts"
             mc:Ignorable="d" hlp:DataContextServices.Activate="True"
             hlp:CompositionServices.ImportViewModel="{x:Static contracts:AppConststands.SpoolView}"
             d:DesignHeight="300" d:DesignWidth="300">
    <TabControl Background="Transparent" ItemsSource="{Binding SpoolTypes}">
        <TabControl.Resources>
            <Style BasedOn="{StaticResource {x:Type TabItem}}" TargetType="{x:Type TabItem}">
                <Setter Property="Header" Value="{Binding Title}" />
                <Setter Property="FontSize" Value="20" />
                <Setter Property="Content" Value="{Binding Converter={hlp:ViewModelConverter EnableCaching=False}}" />
            </Style>
        </TabControl.Resources>
    </TabControl>
</UserControl>